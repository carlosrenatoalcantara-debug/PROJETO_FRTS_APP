/**
 * Shim de re-exportação — FV-UX-003 (F2).
 *
 * A implementação vive em @fortesolar/fv-shared (@fortesolar/fv-shared/inversores), consumida também pelo
 * frontend. Este arquivo existe para preservar o caminho de import histórico do
 * backend. Não contém lógica.
 */
export * from '@fortesolar/fv-shared/inversores'
